# AI Assistant Instructions

When making changes in this repository, always update the pinned reference in `mhi-rc-ex3.yaml` to point to the correct version/commit for the code changes you are introducing.

## Required step for assistant-generated changes
- If you change code under `components/rc_ex3/` or other files that affect the external component behavior, you must also update the relevant pin/ref in `mhi-rc-ex3.yaml` so the YAML references the intended revision.
- Do not leave the YAML pin/ref stale after code updates.
